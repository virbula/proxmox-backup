# Source File Analysis: `bin/proxmox_tape/backup_job.rs`

## Overview
CLI command for tape backup jobs.

## Module Type
**CLI: TapeBackup**

## Key Responsibilities
* Implements functionality located at `bin/proxmox_tape/backup_job.rs`.
* Interacts with the PBS `CLI:` subsystem.
