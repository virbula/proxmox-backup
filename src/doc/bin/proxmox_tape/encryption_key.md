# Source File Analysis: `bin/proxmox_tape/encryption_key.rs`

## Overview
CLI command to manage tape encryption keys.

## Module Type
**CLI: TapeKey**

## Key Responsibilities
* Implements functionality located at `bin/proxmox_tape/encryption_key.rs`.
* Interacts with the PBS `CLI:` subsystem.
