# Source File Analysis: `bin/proxmox_backup_manager/node.rs`

## Overview
CLI command `proxmox-backup-manager node`.

## Module Type
**CLI: Node**

## Key Responsibilities
* Implements functionality located at `bin/proxmox_backup_manager/node.rs`.
* Interacts with the PBS `CLI:` subsystem.
