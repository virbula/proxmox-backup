# Source File Analysis: `bin/proxmox_backup_manager/dns.rs`

## Overview
CLI command `proxmox-backup-manager dns`.

## Module Type
**CLI: DNS**

## Key Responsibilities
* Implements functionality located at `bin/proxmox_backup_manager/dns.rs`.
* Interacts with the PBS `CLI:` subsystem.
