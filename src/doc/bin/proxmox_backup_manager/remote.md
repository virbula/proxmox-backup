# Source File Analysis: `bin/proxmox_backup_manager/remote.rs`

## Overview
CLI command `proxmox-backup-manager remote`.

## Module Type
**CLI: Remote**

## Key Responsibilities
* Implements functionality located at `bin/proxmox_backup_manager/remote.rs`.
* Interacts with the PBS `CLI:` subsystem.
