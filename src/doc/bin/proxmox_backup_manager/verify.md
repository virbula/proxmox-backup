# Source File Analysis: `bin/proxmox_backup_manager/verify.rs`

## Overview
CLI command `proxmox-backup-manager verify-job`.

## Module Type
**CLI: Verify**

## Key Responsibilities
* Implements functionality located at `bin/proxmox_backup_manager/verify.rs`.
* Interacts with the PBS `CLI:` subsystem.
