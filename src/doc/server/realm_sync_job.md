# Source File Analysis: `server/realm_sync_job.rs`

## Overview
Background job to sync users and groups from external directories (LDAP/AD) into the local user database.

## Module Type
**Realm Sync**

## Key Responsibilities
* Implements functionality located at `server/realm_sync_job.rs`.
* Interacts with the PBS `Realm` subsystem.
* Part of the core logic for **Realm Sync**.
