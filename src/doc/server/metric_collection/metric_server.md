# Source File Analysis: `server/metric_collection/metric_server.rs`

## Overview
Background task that collects system metrics and pushes them to configured targets (InfluxDB).

## Module Type
**Metrics Server**

## Key Responsibilities
* Implements functionality located at `server/metric_collection/metric_server.rs`.
* Interacts with the PBS `Metrics` subsystem.
* Part of the core logic for **Metrics Server**.
